# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.local
.env.*.local
# .env.example and .env.mock are committed on purpose: neither holds a secret,
# and .env.mock is what `npm run dev:mock` needs to run the offline demo.

# Supabase CLI machine-local state. Not just noise — supabase/.temp/pooler-url
# holds a database connection string *including the password*.
supabase/.temp/

# Stray print-pipeline export produced while testing the PDF export.
__out.pdf

# The thesis is its own git repository, kept private and pushed separately.
thesis/
